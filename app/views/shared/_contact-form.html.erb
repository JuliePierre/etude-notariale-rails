<%= simple_form_for contact, remote: true, :html => {:class => 'form-horizontal' } do |f| %>
  <div class="row">
    <div class="col s12 m6">
      <%= f.input :nom, :required => true, placeholder: 'Votre nom' %>
    </div>
    <div class="col s12 m6">
      <%= f.input :email, :required => true, placeholder: 'Votre email' %>
    </div>
  </div>
  <div class="row">
    <div class="col s12 m6">
      <%= f.input :telephone, placeholder: 'Votre numéro' %>
    </div>
    <div class="col s12 m6 hide-on-small-only">
      <%= f.input :motif, placeholder: "Motif de demande" %>
<!--       <div class="input-field col">
        <select name="contact[motif]">
          <option value="" disabled selected>Motif de demande</option>
          <option value="Demande de RDV">Demande de RDV</option>
          <option value="Question">Question</option>
        </select>
        <label>Materialize Select</label>
      </div> -->
    </div>
  </div>
  <div class="row">
    <div class="col s12">
      <%= f.input :message, as: :text, placeholder: 'Posez ici votre question' %>
    </div>
  </div>
  <div class= "hidden">
    <%= f.input :nickname, :hint => 'Laissez ce champ vide pour test' %>
  </div>
  <div class="row">
    <div class="col s12">
      <div class="contact-form-button">
        <%= f.button :submit, 'Envoyer ma demande', :class=> "btn btn-primary btn-contact-form" %>
      </div>
    </div>
  </div>
<% end %>
